<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url><loc>{{ request.scheme }}://{{ request.get_host }}{% url 'core:accueil' %}</loc><changefreq>weekly</changefreq><priority>1.0</priority></url>
  <url><loc>{{ request.scheme }}://{{ request.get_host }}{% url 'core:methode' %}</loc><changefreq>monthly</changefreq><priority>0.8</priority></url>
  <url><loc>{{ request.scheme }}://{{ request.get_host }}{% url 'coaching:programmes' %}</loc><changefreq>monthly</changefreq><priority>0.9</priority></url>
  {% for programme in programmes %}
  <url><loc>{{ request.scheme }}://{{ request.get_host }}{% url 'coaching:programme_detail' programme.slug %}</loc><changefreq>monthly</changefreq><priority>0.85</priority></url>
  {% endfor %}
  <url><loc>{{ request.scheme }}://{{ request.get_host }}{% url 'core:tarifs' %}</loc><changefreq>monthly</changefreq><priority>0.8</priority></url>
  <url><loc>{{ request.scheme }}://{{ request.get_host }}{% url 'core:ou_nous_trouver' %}</loc><changefreq>monthly</changefreq><priority>0.7</priority></url>
  <url><loc>{{ request.scheme }}://{{ request.get_host }}{% url 'actualites:actualites' %}</loc><changefreq>weekly</changefreq><priority>0.6</priority></url>
  {% for actualite in actualites %}
  <url><loc>{{ request.scheme }}://{{ request.get_host }}{% url 'actualites:actualite_detail' actualite.slug %}</loc><lastmod>{{ actualite.date_publication|date:"Y-m-d" }}</lastmod><changefreq>monthly</changefreq><priority>0.55</priority></url>
  {% endfor %}
</urlset>
